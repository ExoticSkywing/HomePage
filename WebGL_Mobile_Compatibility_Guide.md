# 移动端 WebGL 与生命周期避坑指南 (Antigravity 架构实战踩雷总结)

这可以说是前端交互开发中极其罕见的三线交叠“史诗级 BUG 综合体”。
在此，将导致 3D 图元在移动端诡异消失的四重地狱及其破局方案做系统性总结，作为本项目的祖传避坑指南：

## 坑位 1：透明画布下的“水泥灰”效应 (Transmission 的折射盲区)
**🕳 踩坑现象**：
移动设备中，为防止手机渲染吃力，曾尝试将 WebGL 画布调整为全透明底（`alpha: true`, `clearColor: 0x00, 0`），以此透出后方的普通网页星空。结果所有具备 `transmission: 1` 物理玻璃质感的材质全都失去了环境光折射，变成了一块扁平、死灰的劣质塑料圈（俗称水泥灰）。

**💡 破局原理**：
WebGL 的物理光线透射算法（`MeshPhysicalMaterial`），它进行折射时提取的是**场景 (Scene) 环境及其背景渲染缓冲区中的像素**，而完全无法直接“透射”它后方属于 HTML 页面的后置兄弟容器内容。当 WebGL 背景设为完全透明时，其内部没有任何可用像素供透射反差取样，从而发生光泽计算剥离。

**🔧 解决对策**：必须使用纯死黑实心画布（`0x000000, 1`）为场景在极限亮度落差下提供极其高对比度的反光底，随后交由 CSS 使用滤色属性 (`mix-blend-mode: screen`) 去除黑色。

---

## 坑位 2：由于强开合成引发的移动端吞层 (WebKit / Z-Fighting)
**🕳 踩坑现象**：
在沿用黑底配合 `mix-blend-mode: screen` 后，电脑可以渲染极限透明水晶玻璃，但在手机 Safari、微信 X5 内核以及省电优化状态下的 WebView 中，整个 `#intro-torus-canvas` 被完全屏藏、隐形了！

**💡 破局原理**：
大部分移动端浏览器的 GPU 合排渲染管线非常保守且存在残缺漏洞。当遇到既带有 WebGL 计算沉重的 Context 层，又需要跨 DOM 进行混色 (`mix-blend-mode`) 时，尤其是当父元素或兄弟节点含有透明度淡入转场动画、或绝对定位渲染管线叠压，极易导致 GPU 分配显存越界（Layer Crash / Z-Fighting）。为防整页宕机，手机浏览器底层引擎往往选择直接冷处理——把带混色的惹事层强制隐去。

**🔧 解决对策**：对含有混色的 WebGL 独立外容器，强加以下四层最强硬件级防护光环，建立隔离结界！
```css
#intro-torus-canvas {
    opacity: 0.999;                  // 强制打破透明通道
    transform: translate3d(0, 0, 0); // 扯出单独的 3D 硬件渲染层，分配独立显存
    will-change: transform;          // 宣告保护状态
    isolation: isolate;              // 终极绝杀：将 mix-blend-mode 的混色限定死于内部作用域！
}
```

---

## 坑位 3：严格模式块级变量冗余声明引发渲染白板
**🕳 踩坑现象**：
在双端控制代码尝试分流配置时，结果不但 3D 圈没出，连网页都全白屏报错。

**💡 破局原理**：
现代原生 ESM / Js 的处理中默认载入 Strict 严格模式。如果同一上下文中 `const isMobile` 被错误地（如按序手误复制）声明了第二次，即便 Gulp 等编译工具无报错直接流到了生产层，浏览器 JS V8 引擎解析到也会直接报 SyntaxError 从而阻断全文读取，导致渲染主线直接崩盘白页。

**🔧 解决对策**：仔细梳理 `const isMobile = window.innerWidth < 800;` 作用域，去重提纯即可。

---

## 坑位 4：Bfcache（往返缓存）+过早 Dispose 引发的幽灵失踪
**🕳 踩坑现象**：
该 BUG 隐蔽性极高。第一次扫码进网页正常渲染，但只要点击切换到内页宇宙，后续不论等过一会或后退再进去加载时。哪怕杀微信后台或不停死划强制刷新，它都要么看运气刷出来，要么变成永远不出来的纯文字页面！

**💡 破局原理**：
原开发者出于极致的“性能洁癖”，在进入（enter）下一页阶段生命监听中，调用了毁灭级的 `cleanup()`，不仅将画板内的几何体、贴图材质全部调用 `.dispose()` 从显卡强制回收砸烂，还用 `display: none` 将其盖棺定论。
不幸的是，现代浏览器（尤其是手机端）由于其极其强大的 **Bfcache (往返缓存/历史栈恢复)** 特性，用户在后退退出主页面退回引导页时，DOM 是直接从内存冻结状态火速取回恢复的。这将直接略过最核心的 `DOMContentLoaded` 生命周期监听器！结果 JS 渲染始化不触发，而 Canvas 在内存中确确实实是一具被亲手清理的“死尸”，由此引发了只在后退或强开页时发生的恶性“幽灵白板”。

**🔧 解决对策**：
引擎早已内建有完善的 `document.addEventListener("visibilitychange")` 函数负责感知用户切出页面并主动阻断 `requestAnimationFrame` 以防耗电。所以在单页状态或类 SPA 切换时根本**无需越俎代庖去强行 `dispose()` 画布**。
直接砍除了 `.enter` 触发时的垃圾清理指令，使其画布常驻于底层显存栈。真正做到退出必停、呼之秒回的丝滑神级无断层体验！

---

## 坑位 5：`<script type="module">` 与 DOMContentLoaded 致命竞态引发的概率性白板
**🕳 踩坑现象**：
哪怕把上述所有渲染死局和大坑全都填平，依然会出现最让人抓狂的“时有时无、加载完全看运气”的灵异现象：甚至在同一设备上，刷新页面时而能出水晶环，时而全是白板。

**💡 破局原理**：
开发者在使用内联模块时，将启动代码置于 `<script type="module">` 内，并为了追求“安全挂载”在内部包裹了 `document.addEventListener('DOMContentLoaded', ...)`。
这是一个极端致命的**生命周期常识陷阱**：在现代浏览器中，`<script type="module">` 天生自带了极强的 `defer`（延迟）属性，它**强制被推迟到整个 HTML DOM 树完整解析之后才开始执行**。
这就引爆了一场极其惨烈的**竞态条件（Race Condition）**：如果当前用户的网络/缓存稍快（通常在本地或强网络下），`DOMContentLoaded` 的全局事件广播，早就在该 `module` 模块脚本跑到监听器那一行之前就已经触发结束了。
此时你的模块慢悠悠地再去挂载该监听器，无异于“刻舟求剑”——这趟末班车早开走了。你的回调函数将永远等不到信号被执行，导致最核心的 3D 初始化函数彻底胎死腹中！

**🔧 解决对策**：
认清模块化脚本天然的加载时序优势。大胆剥除那层画蛇添足且成为绊脚石的生命周期监听外壳！直接检查对应 DOM 并**直接裸调** `init()` 函数！因为在 `type="module"` 被执行的时刻里，主树及前置 DOM 节点绝对已经是 100% 绝对安全且就位了的。
